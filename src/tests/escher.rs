// use crate::solidity::compile_path_and_get_findings;
// use crate::walker::sort_findings_by_len;

#[test]
fn escher_integration() {
    // let findings = compile_path_and_get_findings("test-data/escher/", None);

    // let sorted_findings = sort_findings_by_len(&findings);

    // sorted_findings.iter().for_each(|(n, mfs)| {
    //     // println!("{n}: {}", mfs.len());
    //     if mfs.len() > 100 {
    //         mfs.iter()
    //             .enumerate()
    //             .filter(|(i, _)| i > &10 && i <= &15)
    //             .for_each(|(_, mf)| {
    //                 // println!("{:#?} {:#?} {}", &mf.meta, mf.finding.src, mf.finding.code);
    //             });
    //     }
    // });
}
